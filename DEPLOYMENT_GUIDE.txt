
═══════════════════════════════════════════════════════════════════════════════
                    MSS DEPLOYMENT - COMPLETE AUTOMATION GUIDE
═══════════════════════════════════════════════════════════════════════════════

🎯 WHAT I'VE DONE FOR YOU:

✓ Analyzed all your deployment files
✓ Fixed Dockerfile.app (optimized, removed redundant steps)
✓ Fixed entrypoint-app.sh (simplified, removed runtime installs)
✓ Fixed GitHub workflow (corrected health check endpoint)
✓ Created automation script (Deploy-MSS.ps1)
✓ Created fixed file versions for backup

═══════════════════════════════════════════════════════════════════════════════
                              📋 EXACT STEPS TO FOLLOW
═══════════════════════════════════════════════════════════════════════════════

STEP 1: DOWNLOAD ALL FILES
───────────────────────────────────────────────────────────────────────────────
Download these files from the chat (they're ready to download above):

  1. Deploy-MSS.ps1              ← Main automation script
  2. Dockerfile.app.FIXED        ← Fixed Dockerfile
  3. entrypoint-app.sh.FIXED     ← Fixed entrypoint
  4. gcp-deploy.yml.FIXED        ← Fixed GitHub workflow

Save them to: G:\Users\daveq\MSS\


STEP 2: OPEN POWERSHELL AS ADMINISTRATOR
───────────────────────────────────────────────────────────────────────────────
1. Press Windows Key
2. Type "PowerShell"
3. Right-click "Windows PowerShell"
4. Select "Run as Administrator"


STEP 3: NAVIGATE TO YOUR PROJECT
───────────────────────────────────────────────────────────────────────────────
In PowerShell, run:

    cd G:\Users\daveq\MSS


STEP 4: RUN THE AUTOMATION SCRIPT (DRY RUN FIRST)
───────────────────────────────────────────────────────────────────────────────
Test what the script will do WITHOUT making changes:

    .\Deploy-MSS.ps1 -All -DryRun

This shows you what will be fixed without actually changing anything.


STEP 5: RUN THE AUTOMATION SCRIPT (ACTUAL FIX)
───────────────────────────────────────────────────────────────────────────────
Now apply all fixes:

    .\Deploy-MSS.ps1 -All

This will:
  ✓ Fix Dockerfile.app
  ✓ Fix docker/entrypoint-app.sh
  ✓ Fix .github/workflows/gcp-deploy.yml
  ✓ Validate your environment
  ✓ Test Docker build locally


STEP 6: REVIEW THE CHANGES
───────────────────────────────────────────────────────────────────────────────
The script will show you:
  • What files were fixed
  • Any environment issues
  • Docker build results
  • Health check status


STEP 7: COMMIT AND PUSH TO GITHUB
───────────────────────────────────────────────────────────────────────────────
After the script completes successfully:

    git add .
    git commit -m "Fix deployment configuration"
    git push origin main


STEP 8: VERIFY GITHUB ACTIONS
───────────────────────────────────────────────────────────────────────────────
1. Go to: https://github.com/dquillman/MSS/actions
2. Watch the deployment workflow run
3. Check for any errors
4. Get your Cloud Run URL from the workflow output


═══════════════════════════════════════════════════════════════════════════════
                           🔧 ALTERNATIVE: MANUAL STEPS
═══════════════════════════════════════════════════════════════════════════════

If you prefer to do it manually (or if the script has issues):

MANUAL STEP 1: Replace Dockerfile.app
───────────────────────────────────────────────────────────────────────────────
    copy Dockerfile.app.FIXED Dockerfile.app


MANUAL STEP 2: Replace entrypoint script
───────────────────────────────────────────────────────────────────────────────
    mkdir docker
    copy entrypoint-app.sh.FIXED docker\entrypoint-app.sh


MANUAL STEP 3: Replace GitHub workflow
───────────────────────────────────────────────────────────────────────────────
    mkdir .github\workflows
    copy gcp-deploy.yml.FIXED .github\workflows\gcp-deploy.yml


MANUAL STEP 4: Test Docker build
───────────────────────────────────────────────────────────────────────────────
    docker build -f Dockerfile.app -t mss-test:latest .
    docker run -d -p 8080:8080 -e PORT=8080 mss-test:latest

    # Wait 10 seconds, then test:
    curl http://localhost:8080/health

    # Stop the container:
    docker ps
    docker stop <container_id>


MANUAL STEP 5: Commit and push
───────────────────────────────────────────────────────────────────────────────
    git add .
    git commit -m "Fix deployment configuration"
    git push origin main


═══════════════════════════════════════════════════════════════════════════════
                              🔍 WHAT WAS FIXED
═══════════════════════════════════════════════════════════════════════════════

DOCKERFILE FIXES:
  ✓ Removed redundant requirements.txt copies
  ✓ Simplified build process (single stage)
  ✓ Removed unnecessary PATH manipulations
  ✓ Added proper health check
  ✓ Optimized layer caching

ENTRYPOINT FIXES:
  ✓ Removed runtime pip installs (all deps in Dockerfile)
  ✓ Simplified dependency checks
  ✓ Removed complex fallback logic
  ✓ Cleaner error handling
  ✓ Proper exec for gunicorn

WORKFLOW FIXES:
  ✓ Fixed health check endpoint (/health instead of /healthz)
  ✓ Updated authentication method
  ✓ Added proper permissions
  ✓ Improved error handling
  ✓ Added deployment verification


═══════════════════════════════════════════════════════════════════════════════
                           ⚠️ IMPORTANT PREREQUISITES
═══════════════════════════════════════════════════════════════════════════════

BEFORE DEPLOYING, ENSURE YOU HAVE:

1. GitHub Secret: GCP_SA_KEY
   ─────────────────────────────────────────────────────────────────────────
   • Go to: https://github.com/dquillman/MSS/settings/secrets/actions
   • Click "New repository secret"
   • Name: GCP_SA_KEY
   • Value: Your GCP service account JSON (entire file contents)

2. Google Cloud APIs Enabled
   ─────────────────────────────────────────────────────────────────────────
   • Cloud Run API
   • Container Registry API
   • Cloud Build API

3. Docker Installed
   ─────────────────────────────────────────────────────────────────────────
   • Download from: https://www.docker.com/products/docker-desktop
   • Make sure it's running

4. Required Files Present
   ─────────────────────────────────────────────────────────────────────────
   • avatar_library.json
   • intro_outro_library.json
   • logo_library.json
   • thumbnail_settings.json


═══════════════════════════════════════════════════════════════════════════════
                              🐛 TROUBLESHOOTING
═══════════════════════════════════════════════════════════════════════════════

ISSUE: "Docker build failed"
───────────────────────────────────────────────────────────────────────────────
SOLUTION:
  • Make sure Docker Desktop is running
  • Check that all required files exist
  • Try: docker system prune -a (cleans up old images)


ISSUE: "Health check failed"
───────────────────────────────────────────────────────────────────────────────
SOLUTION:
  • Wait longer (app takes ~30 seconds to start)
  • Check logs: docker logs <container_id>
  • Verify port 8080 is not in use


ISSUE: "GitHub Actions deployment failed"
───────────────────────────────────────────────────────────────────────────────
SOLUTION:
  • Check GCP_SA_KEY secret is set correctly
  • Verify GCP APIs are enabled
  • Check workflow logs for specific error


ISSUE: "Permission denied on entrypoint.sh"
───────────────────────────────────────────────────────────────────────────────
SOLUTION:
  • The Dockerfile sets chmod +x automatically
  • If still failing, check file line endings (should be LF, not CRLF)


═══════════════════════════════════════════════════════════════════════════════
                           📞 NEED MORE HELP?
═══════════════════════════════════════════════════════════════════════════════

If you encounter issues:

1. Run the script with -DryRun to see what it will do
2. Check the output for specific error messages
3. Review the GitHub Actions logs
4. Check Cloud Run logs in GCP Console


═══════════════════════════════════════════════════════════════════════════════
                              ✅ SUCCESS CHECKLIST
═══════════════════════════════════════════════════════════════════════════════

After deployment, verify:

□ GitHub Actions workflow completed successfully
□ Cloud Run service is running
□ Health check endpoint responds: <your-url>/health
□ Application loads: <your-url>/
□ No errors in Cloud Run logs


═══════════════════════════════════════════════════════════════════════════════
                                  🎉 YOU'RE DONE!
═══════════════════════════════════════════════════════════════════════════════

Your MSS application should now be:
  ✓ Properly containerized
  ✓ Deployed to Google Cloud Run
  ✓ Accessible via HTTPS
  ✓ Auto-scaling based on traffic
  ✓ Health-checked and monitored

═══════════════════════════════════════════════════════════════════════════════
